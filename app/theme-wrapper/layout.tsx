export const metadata = {
  title: "Theme wrapper — STS Planner",
  description:
    "Standalone theme reference (examples + docs) and exportable HTML/CSS pack.",
};

export default function ThemeWrapperLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
